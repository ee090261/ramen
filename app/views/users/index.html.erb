

<div>
  <% @users.each do |user| %>
  <tr>
  	<td><%= attachment_image_tag(user, :user_image, :fill, 50, 50, fallback: "no-image-mini.jpg") %></td>
  	<td><%= user.name %></td>
	<td><%= link_to "Show", user_path(user), class: "user_#{user.id}" %></td>
  </tr>
  <% end %>
</div>

